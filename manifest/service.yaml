apiVersion: v1
kind: Service
metadata:
  name: java-application-svc
  namespace: argocd-demo
spec:
  type: ClusterIP
  selector:
    app: java-maven-app
  ports:
    - port: 80
      targetPort: 8080

apiVersion: v1
kind: Service
metadata:
  name: java-applications-svc
  namespace: argocd-demo
spec:
  type: ClusterIP
  selector:
    app: java-maven-apps
  ports:
    - port: 80
      targetPort: 8080
